@using Microsoft.AspNetCore.Http
@inject IHttpContextAccessor httpContextAccessor

@{
    //var myClassList = httpContextAccessor.HttpContext.Session.GetString("tList") != null ? "active" : "inactive"; //Session["MyticketList"] != null ? "active" : "inactive";
    string myClassList="";
}

<li>
    <a href="@Url.Action("NFChangePhone", "InjaKalaCustomerValidation")" class="@(myClassList)">
        تغییر شماره موبایل
    </a>
</li>
<script>
    $('body').addClass('customer-pages-body');
</script>